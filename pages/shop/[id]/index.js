import React from 'react'
import Menu from '../../../components/Menu'
const menu = ({isLog,setLog,isGame,setGame}) => {
    return (
        <div>
            <Menu></Menu>
        </div>
    )
}

export default menu
